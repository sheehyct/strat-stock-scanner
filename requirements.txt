# MCP Server (official SDK). Pinned to the resolver-friendly minor that
# satisfies httpx==0.27.2 + pydantic==2.9.2 without backtracking through
# 20+ wheel downloads at build time.
mcp>=1.12,<1.13

# FastAPI for OAuth endpoints (starlette is auto-installed by fastapi)
fastapi==0.115.0
uvicorn[standard]==0.32.0

# HTTP client
httpx==0.27.2

# Configuration & validation
pydantic==2.9.2
pydantic-settings==2.5.2

# OAuth & JWT
authlib==1.3.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
itsdangerous==2.1.2

# Timezone handling
pytz>=2024.1

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
